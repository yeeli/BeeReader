import * as AppActions from '~/actions/app'

const defaultState = {
}

const App = (state = defaultState, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default App
